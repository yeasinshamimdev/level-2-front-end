import { LoadingOutlined } from "@ant-design/icons";

const Loading = () => {
  return (
    <div
      style={{
        margin: "auto",
      }}
    >
      <LoadingOutlined size={60} />
    </div>
  );
};

export default Loading;
